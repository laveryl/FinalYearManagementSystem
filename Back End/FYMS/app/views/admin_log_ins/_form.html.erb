<%= form_for(@admin_log_in) do |f| %>
  <% if @admin_log_in.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_log_in.errors.count, "error") %> prohibited this admin_log_in from being saved:</h2>

      <ul>
      <% @admin_log_in.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
